# 更新日志

本文件记录每个版本的实质性变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [Unreleased]

### 新增

**笔记（网页端）**
- 「写段字」升级为「笔记」：可保存、可新建多份、可按分类管理，像个小笔记软件
- 富文本编辑：标题三级 / 加粗 / 斜体 / 下划线 / 字号 / 有序无序列表 / 引用 / 分割线
- 停手 1.5 秒自动保存；标题可留空，自动取第一行
- 新增 `htmlnote.py`：PIL 直接排版笔记 HTML 为打印页面（自动换行、按内容分页），
  预览与打印走同一份渲染，不依赖浏览器

**手机中转（安卓 APP + 网页端）**
- 新增 `android-relay/`：极简安卓 APP，注册为系统分享接收者
  （微信 / 浏览器 / 相册 / WPS 点分享 → 选「打印中转」）
- 接收的内容自动存进手机 `Download/手机打印收件箱/`，并自动 POST 到电脑收件箱
- 零第三方依赖、零存储权限（Android 10+ 走 MediaStore）、透明接收页收完即退
- 网页端新增「手机中转」面板：4 秒自动轮询、新文件标记、多选加入打印队列

## [v1.0.0] — 2026-09-18

首个公开版本。

### 新增

**打印能力**
- 手机浏览器上传打印：图片 / PDF / Word / Excel / PPT / 纯文本
- 随手记：手机直接输入文字打印，可选字号
- 多图排版（截图拼版）：拖动、缩放、1/2/4 宫格
- 原格式直印：Office 走 COM 原生 `PrintOut`，完全保留原文件的边距与分页
- 重排放大：自动检测内容边界、剥掉白边后等比放大到设定页边距
- 纸张 / 方向 / 质量可选，纸张范围按打印机驱动能力动态过滤
- 打印测试页、异步任务进度轮询

**微信自助打印**
- 文件：扫描微信 PC 端落盘文件，支持按会话、按时间（收件箱模式）过滤
- 图片：列出本地明文缓存图（缩略图 / 收藏 / 迁移），带清晰度评级与建议纸张
- 只复制不移动，不读聊天数据库，不注入微信进程

**工程**
- 自动识别真实打印机（按端口判定，排除虚拟 PDF 打印机）
- 自检脚本 `selftest.py`（不耗材验证整条打印链路）
- 便携版打包 `build_portable.py`：嵌入式 Python + 依赖 + 源码，免装即用的绿色文件夹
- 完整文档（docs/ 共 8 篇）

### 修复

- 双重边距：PDF 自带白边被二次缩放导致内容偏小 → 新增 reflow 模式
- dpi 计算 min/max 写反，糊图被误判为清晰 → 修正并据此给出清晰度评级
- 微信图片会话过滤错误，无归属图片混入指定会话 → 非指定会话时只取带归属的图
- 虚拟打印机误判（按名字猜）→ 改为按端口判定
- 便携版 `._pth` 丢失标准库 zip 导致启动崩溃 → 保留 `python313.zip`
- 便携版 pywin32 依赖不全 → 补 `pywin32.pth`、`pythoncom313.dll` 改名 `.pyd`
- 便携版用 `pythonw.exe` 导致看不到网址 → 改回 `python.exe`
- 端口重复绑定导致请求被随机分发 → 启动前探测 `/health`，已有实例则主动退出

### 已知限制

- 仅支持 Windows（依赖 pywin32 / GDI / Office COM）
- 微信聊天原图为 AES 加密存储，离线不可解，只能取明文缓存版本
- Office 原格式直印需要本机安装 Microsoft Office
